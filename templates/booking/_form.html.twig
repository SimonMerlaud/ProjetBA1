<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
{{ form_start(form) }}

    {{ form_label(form.beginAt, 'Date de début') }}
    {{ form_widget(form.beginAt) }}
    {{ form_errors(form.beginAt) }}

    {{ form_label(form.endAt, 'Date de fin') }}
    {{ form_widget(form.endAt) }}
    {{ form_errors(form.endAt) }}

    <a class="btn btn-primary mt-2" href="{{ path('booking_calendar') }}">retour</a>
    <button class="btn btn-primary mt-2">{{ button_label|default('Valider') }}</button>

{{ form_end(form) }}
</div>