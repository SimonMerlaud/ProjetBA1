
{% block body %}
    <div class="row justify-content-center">
        <div class="col-8">
            <table class="table table-hover text-center">
                <thead>
                <tr>
                    <th scope="col">Nom</th>
                    <th scope="col">Adresse</th>
                </tr>
                </thead>
                <tbody>

                {% if associations is null%}
                        <h3>Il n'y a pas d'association a ce nom</h3>
>                    {% endif %}

                    {% for association in associations %}
                            <tr class="table-light" style="cursor: pointer" onclick="window.location='{{ url("association_view",{'id': association.id}) }}'">
                                <th scope="row">
                                {{ association.nom }}
                            </th>
                            <td>{{ association.adresse }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    {{ include('association/pagination.html.twig', {'nbPage': nbPage,'currentPage': currentPage}) }}

{% endblock %}

